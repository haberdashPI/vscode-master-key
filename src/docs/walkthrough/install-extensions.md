Once the bindings have been activated, you will need to install any associated extensions. A
notification should show up saying as much. Click the `Install Extensions` button. You will
be provided a list of extensions to install. Click the check box at the very top to select
all extensions and the click "Ok".
